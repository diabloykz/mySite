<h1 align="center"><%= "Categorie: " + @category.name %></h1>
<div align="center">
  <% if logged_in? && current_user.admin? %>
    <div class="row">
      <span class="badge"><%= link_to "Éditer le nom de catégorie", edit_category_path(@category) %></span>
    </div>
  <% end %>
  <%= will_paginate @category_articles %>
</div>
<%= render 'articles/article' , object: @category_articles%>
<div align="center">
  <%= will_paginate @category_articles %>
</div>
